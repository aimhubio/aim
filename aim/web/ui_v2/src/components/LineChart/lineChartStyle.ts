import { makeStyles } from '@material-ui/core';

export default makeStyles(() => ({
  chart: {
    backgroundColor: 'white',
    height: '100%',
    width: '100%',
    position: 'relative',
    overflow: 'hidden',
    userSelect: 'none',
  },
}));
